{% extends 'base.html.twig' %}

{% block title %}Hello DBController!{% endblock %}

{% set users = [{name : 'John'}, {name : 'Susan'}, {name : 'Robert'}] %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name|upper }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/test/src/Controller/DBController.php'|file_link(0) }}">src/Controller/DBController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/test/templates/db/index.html.twig'|file_link(0) }}">templates/db/index.html.twig</a></code></li>
    </ul>
    <ul> 
        {% for user in users %} 
            <li> {{user.name}} you won {{random_gift[loop.index0]}} </li>
        {% endfor %}
    </ul>

    {% for user in users %} 
            <p> {{user.name}} </p>
        {% endfor %}

    {% for message in app.flashes('notice') %} 
        <div class = "flash-notice">
            {{message}}
        </div>
    {% endfor %} 

    <p>The google tracking code is: {{ ga_code }}</p>

    {{app.user}}
    {{app.request.get('param')}}
    {{app.environment}}
    {{dump(app.session)}}


    {# {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }} #}

    {{ form(form) }}

    {{%trans%}} 
        test de proba
    {{%endtrans%}}


</div>
{% endblock %}
